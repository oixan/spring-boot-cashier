<?xml version="1.0" encoding="UTF-8"?>
<settings xmlns="http://maven.apache.org/SETTINGS/1.0.0"
          xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xsi:schemaLocation="http://maven.apache.org/SETTINGS/1.0.0 http://maven.apache.org/xsd/settings-1.0.0.xsd">

  <!-- Configurazione dei server di deployment -->
  <servers>
    <server>
      <id>central</id>
      <username>${SONATYPE_TOKEN_USERNAME}</username>
      <password>${SONATYPE_TOKEN_PASSWORD}</password>
    </server>
  </servers>

  <!-- Configurazione dei mirror per Maven Central (opzionale) -->
  <mirrors>
    <mirror>
      <id>central</id>
      <mirrorOf>central</mirrorOf>
      <url>https://repo.maven.apache.org/maven2</url>
      <blocked>false</blocked>
    </mirror>
  </mirrors>



  <!-- Configurazione di profili di build (opzionale) -->
  <profiles>
    <profile>
      <id>default</id>
      <activation>
        <activeByDefault>true</activeByDefault>
      </activation>
      <repositories>
        <repository>
          <id>central</id>
          <url>https://repo.maven.apache.org/maven2</url>
        </repository>
      </repositories>
    </profile>
  </profiles>

  <!-- Abilitazione per l'esecuzione di profile specifici -->
  <activeProfiles>
    <activeProfile>default</activeProfile>
  </activeProfiles>

</settings>
